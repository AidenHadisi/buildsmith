{
  "name": "buildsmith",
  "version": "0.0.0",
  "private": true,
  "description": "A build pipeline for coding agents that they can't skip: spec, critique, review, slices, verification, all derived from markdown on disk.",
  "keywords": [
    "agents",
    "bun",
    "cursor",
    "kanban",
    "mcp",
    "spec-driven-development"
  ],
  "homepage": "https://github.com/AidenHadisi/buildsmith#readme",
  "bugs": {
    "url": "https://github.com/AidenHadisi/buildsmith/issues"
  },
  "license": "MIT",
  "author": {
    "name": "Aiden Hadisi",
    "url": "https://github.com/AidenHadisi"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AidenHadisi/buildsmith.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun run --filter @buildsmith/cli dev",
    "build": "bun run --filter @buildsmith/cli build",
    "check": "oxlint && oxfmt --check",
    "lint": "oxlint .",
    "fmt": "oxfmt --write",
    "fmt:check": "oxfmt --check",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "release": "bumpp"
  },
  "dependencies": {
    "@buildsmith/cli": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "1.4.2",
    "bumpp": "12.3.0",
    "oxfmt": "0.67.0",
    "oxlint": "1.82.0",
    "typescript": "catalog:"
  },
  "engines": {
    "bun": ">=1.4.2"
  },
  "packageManager": "bun@1.4.2",
  "catalog": {
    "typescript": "7.0.2",
    "zod": "4.6.2"
  }
}
